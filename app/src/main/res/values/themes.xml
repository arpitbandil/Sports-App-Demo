<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SportsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="Theme.SportsApp" parent="Base.Theme.SportsApp" />

    <style name="TabLayoutText" parent="TextAppearance.AppCompat.Widget.Button">
        <item name="textAllCaps">true</item>
        <item name="tabTextColor">@color/dark_gray</item>
    </style>
</resources>